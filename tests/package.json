{
  "name": "scula-lu-miron-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Ad-hoc Playwright checks for editor.html, recipes.html, index.html, calendar.html, map.html, voice.html and song.html (see README.md). Not part of the apps themselves - see the root CLAUDE.md for why the apps stay dependency-free.",
  "scripts": {
    "test": "for f in flow stuck gestures stress regression desktop extras withimage infinite undoredo spline spline-touch polyline recipes mealplan targets graph cause find nav wbrename wbsaveall wbadopt wbresume wbtodo taskstatus gantt gdsync importance idea mdundo mdlines paste codecopy color timeline media map calendar drive voice melody song dictate transfer; do node $f.js || exit 1; done"
  },
  "dependencies": {
    "playwright": "^1.62.1"
  }
}
