{
  "name": "scula-lu-miron-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Ad-hoc Playwright checks for editor.html, recipes.html, index.html, calendar.html, map.html and voice.html (see README.md). Not part of the apps themselves - see the root CLAUDE.md for why the apps stay dependency-free.",
  "scripts": {
    "test": "for f in flow stuck gestures stress regression desktop extras withimage infinite undoredo spline spline-touch polyline recipes mealplan targets graph cause find nav wbrename wbsaveall wbresume wbtodo importance idea mdundo mdlines paste codecopy color timeline map calendar drive voice melody dictate transfer; do node $f.js || exit 1; done"
  },
  "dependencies": {
    "playwright": "^1.62.1"
  }
}
